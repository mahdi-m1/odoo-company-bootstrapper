# تعليمات الاستخدام — Odoo Company Bootstrapper (البحرين)

برنامج لإنشاء هيكل شركة كامل داخل **أودو** تلقائياً، مع دعم **نظام الساعات**، **المهن والمنتجات الديناميكية**، **تأمينات SIO البحرين**، وواجهة ويب عربية على **المنفذ 80**.

المستودع: [github.com/mahdi-m1/odoo-company-bootstrapper](https://github.com/mahdi-m1/odoo-company-bootstrapper)

---

## 1. المتطلبات

| المتطلب | التفاصيل |
|---------|----------|
| Python | 3.10 أو أحدث |
| سيرفر أودو | مع إمكانية الاتصال عبر XML-RPC |
| حساب أودو | يُفضّل **admin** أو مدير نظام (صلاحيات تثبيت التطبيقات) |
| الذكاء الاصطناعي (اختياري) | Claude CLI أو مفتاح Anthropic / OpenAI أو Ollama |

---

## 2. التثبيت

```bash
git clone https://github.com/mahdi-m1/odoo-company-bootstrapper.git
cd odoo-company-bootstrapper

python3 -m venv .venv
source .venv/bin/activate          # ويندوز: .venv\Scripts\activate

pip install -e .
```

### (موصى به) تثبيت Claude CLI

```bash
npm install -g @anthropic-ai/claude-code
```

أو استخدم مزوداً آخر:

```bash
export ANTHROPIC_API_KEY=sk-ant-...
# أو
export OPENAI_API_KEY=sk-...
```

---

## 3. التشغيل

### واجهة الويب (موصى بها)

```bash
odoo-bootstrap ui
# أو بدون root:
odoo-bootstrap ui --port 8080
```

ثم افتح: `http://عنوان-السيرفر/` أو `http://localhost:8080/`

### سطر الأوامر

```bash
odoo-bootstrap init
odoo-bootstrap test
odoo-bootstrap plan
odoo-bootstrap run
odoo-bootstrap run --dry-run
```

---

## 4. صفحات واجهة الويب

| الصفحة | الوظيفة |
|--------|---------|
| **الإعدادات** | رابط أودو، قاعدة البيانات، المستخدم، كلمة المرور، مزود الذكاء الاصطناعي |
| **الشركة** | الاسم، CR، العنوان، نظام العمل، الفروع |
| **المهن** | المهن + سعر الساعة الاختياري |
| **المنتجات** | منتجات أو خدمات (Hours للساعة) |
| **الموظفون** | شهري أو hourly |
| **تشغيل** | توليد الخطة ثم التنفيذ |

---

## 5. ماذا يحدث عند التنفيذ؟

### ① التجهيز التلقائي

1. التحقق من صلاحيات **Admin** ومحاولة منحها إن لزم.
2. تثبيت الموديولات الناقصة: `hr`, `hr_contract`, `project`, `product`, `sale_management`, `account`, `contacts`, `mail`.

### ② إنشاء البيانات

الشركة، الفروع، الأقسام، المهن، الموظفون، المشاريع، الأصناف.

> استخدم حساب **admin** في الإعدادات.

---

## 6. نظام الساعات والمهن

**المهن:** `الاسم | سعر الساعة | القسم`

**الموظفون:** `الاسم | المهنة | monthly أو hourly | القيمة | ساعات متوقعة | القسم`

**المنتجات:** `الاسم | service/product | Hours/Units | السعر`

---

## 7. تأمينات البحرين (SIO 2026)

| النوع | موظف | شركة | المجموع |
|--------|------|------|---------|
| بحريني | 8% | 18% | **26%** |
| مغترب | 1% | 3%+EOSB | حسب الحالة |

---

## 8. مزودو الذكاء الاصطناعي

| المزود | الإعداد |
|--------|---------|
| Claude CLI | `provider: claude` |
| Anthropic | `provider: anthropic` + `ANTHROPIC_API_KEY` |
| OpenAI | `provider: openai` + `OPENAI_API_KEY` |
| Ollama | `provider: ollama` |

---

## 9. المنفذ 80 على Linux

```bash
sudo odoo-bootstrap ui
# أو
sudo setcap 'cap_net_bind_service=+ep' $(readlink -f $(which python3))
odoo-bootstrap ui
```

---

## 10. استكشاف أخطاء شائعة

| المشكلة | الحل |
|---------|------|
| فشل الاتصال | تحقق من الرابط وقاعدة البيانات والمستخدم |
| فشل تثبيت موديول | استخدم admin |
| رفض المنفذ 80 | `--port 8080` أو sudo |
| فشل الذكاء الاصطناعي | خطة احتياطية تلقائية أو ضبط API |

---

## الترخيص

MIT
